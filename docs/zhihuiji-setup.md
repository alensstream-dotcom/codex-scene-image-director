# 智绘姬设置与原位按钮

已核验 st-chatu8 2.7.7：真实生成按钮为 `.st-chatu8-image-button`，加载态为 `data-loading="true"`。

推荐设置：

- 模式：ComfyUI。
- 开始标记：`[`；结束标记：`]`。
- 关闭 LLM 扩写、智能分析正文、二次重写 Prompt、自动改变角色与按剧情切换风格。
- 固定质量词放在智绘姬或 ComfyUI；不要把 `second girl` 永久加入全局负面。

按钮位置规则：

1. JANIMA v8.0 每轮输出3–6个 `[prompt]`，并分别紧跟在对应剧情段落后。
2. 智绘姬在该字符位置创建真实按钮。
3. 救援器只添加原位布局标记，不复制、不新建替代按钮。
4. 救援器会忽略并隐藏 `[Unnamed Persona]` 等不符合图像标签格式的误按钮。
4. 点击后图片仍在同一剧情位置生成。

如果按钮仍堆在底部，先检查原始回复是否把多个 `[prompt]` 堆在末尾；这属于世界书输出位置问题，而不是按钮 CSS。v7.9 已针对这一点加入硬规则。
