import { readFile, writeFile } from 'node:fs/promises';

const sourcePath = new URL('../worldbooks/JANIMA_v8_2_Galgame_Director.json', import.meta.url);
const outputPath = new URL('../worldbooks/JANIMA_v8_3_Galgame_Director.json', import.meta.url);
const worldbook = JSON.parse(await readFile(sourcePath, 'utf8'));

worldbook.entries['2'].comment = 'DNA注入提示-JANIMA v8.3 Galgame连续性锁';
worldbook.entries['3'].comment = '[mvu_update]DNA/场景/分镜锚点更新规则-v8.3';
worldbook.entries['3'].content = worldbook.entries['3'].content.replaceAll('v8.2', 'v8.3');
worldbook.entries['4'].comment = '本体-JANIMA v8.3手机Galgame整轮事件分镜导演-去重复/后半段覆盖/身份连续';

let content = worldbook.entries['4'].content
    .replaceAll('JANIMA_v8_2_MOBILE_GALGAME_DIRECTOR', 'JANIMA_v8_3_MOBILE_GALGAME_STORYBOARD_DIRECTOR')
    .replace('世界书负责选镜与写 Prompt；插件只在后台拦错、补漏和保持原位按钮。', '世界书先规划整轮独立事件，再选镜与写 Prompt；插件在后台复核事件覆盖、合并重复动作、重排前密后疏的按钮，并保持原位显示。');

content = content.replace('# 一、每张图的取材窗口【最重要】', `# 零、整轮事件表【输出前必须静默完成】
- 先构思完整正文，再按时间顺序列出本轮所有“可见女性事件 beat”；完成整轮事件表后才决定 Prompt 数量与插入点，禁止边写第一段边把图片额度提前用完。
- “新 beat”必须至少改变一项剧情事实：核心物理动作、动作施受关系、情绪方向、可见人物、地点、服装/裸露状态、关键道具状态或冲突阶段。只改变镜头远近、光线、措辞或同一动作的细小过程，不算新 beat。
- 连续数段若仍是同一次拥抱、亲吻、抚摸、递物、站姿、追逐、攻击、对话反应、相同体位或相同高潮阶段，全部合并为一个事件组，只允许一个 Prompt。禁止用 close-up / wide shot 等摄影变化把一个动作伪装成多张图。
- 对事件组按“关系/冲突后果、女性动作与表情清晰度、状态变化、画面感”评分，每组最多选一个赢家镜头；普通重复反应直接淘汰。
- 把整轮分成开场、中段、结尾三个时间区检查覆盖。只要中后段存在新的高价值女性 beat，就必须为它保留插入位；不得把全部或绝大多数 Prompt 消耗在前半段。
- 最后一张 Prompt 后必须再次扫描剩余正文：若发生新的女性动作、情绪反转、人物登退场、地点/服装/道具/关系变化，必须补最后一张；若只是延续同一动作，则不补。
- 图片数由“独立高价值事件组”决定，不由段落数决定：只有 1 个事件组就 1 张，2 个就 2 张；通常有 3 个不同事件时用 3 张；只有确有 4–6 个不同事件组才增加到 4–6 张；上限 6。

# 一、每张图的取材窗口【最重要】`);

content = content.replace(`# 四、Galgame 出图节奏
- 正常含女性的剧情目标 3 张：尽早建立女性状态、核心互动高点、结尾变化。写正文时自然提供三个不同的可画 beat，不用重复图凑数。
- 新女性登场、地点/服装/道具变化、强表情或动作阶段变化时可增至 4 张；追逐、战斗、多地点连续转场可用 5 张；确有六个互不重复的女性高价值 beat 才用 6 张。
- 若正文实际只有 1–2 个女性可画瞬间，就只给 1–2 张；剧情真实和不重复优先于最低数量。硬上限 6 张。
- 第一张应在第一个完整女性视觉 beat 后尽快出现；结尾若在上一张图后又发生新女性关键 beat，必须补上，不得留下大段可画剧情没有按钮。`, `# 四、Galgame 出图节奏
- 不设必须凑满的最低数量。先合并连续动作，再按独立高价值事件组计数；通常完整回复会自然得到 3 张，但绝不能为了达到 3 张重复同一动作。
- 新女性登场、地点/服装/道具变化、强表情、关系反转或动作阶段真正变化时可增至 4 张；追逐、战斗、多地点连续转场可用 5 张；确有六个互不重复的女性高价值事件组才用 6 张。
- 若正文实际只有 1–2 个独立女性可画事件，就只给 1–2 张；剧情真实、动作差异和时间覆盖优先于数量。硬上限 6 张。
- 第一张在第一个完整女性视觉 beat 后出现；第二、三张不能只是第一张的机位变化。结尾若在上一张后又发生新女性关键 beat，必须补上，不得留下后半段大段可画剧情没有按钮。
- 三张及以上时，除非后半段确实没有新 beat，否则至少一张必须来自正文后 40%；四张及以上时至少两张来自正文后半段。`);

content = content.replace('成人剧情仍执行同一节奏：正常目标 3 张，确有独立动作阶段、姿势变化、地点变化或情绪转折时用 4–6 张；', '成人剧情仍执行同一事件组节奏：持续的相同动作或体位只用 1 张，确有独立动作阶段、体位变化、地点变化或情绪转折时才用 2–6 张；');
content = content.replace('提交前逐张静默确认：取材窗口正确；选的是窗口内最佳女性画面；没有纯男/纯场景；没有未来信息；人物与当前动作服装地点一致；重复女性身份标签与上一张一致；多人已分块定位；计数准确。', '提交前逐张静默确认：取材窗口正确；每张核心动作签名互不重复；没有两个 Prompt 属于同一持续事件组；开场/中段/结尾覆盖合理；最后一张之后没有遗漏的新女性关键 beat；没有纯男/纯场景；没有未来信息；人物与当前动作服装地点一致；重复女性身份标签与上一张一致；多人已分块定位；计数准确。');

worldbook.entries['4'].content = content;
await writeFile(outputPath, `${JSON.stringify(worldbook, null, 2)}\n`, 'utf8');
